function sortedSquaredArray(array) {
    var squaredNums = []
    var smallerIdx = 0
    var largerIdx = array.length -1

    for(const num in array){
        
    }

    //   for(const num in array){
    //       const value = array[num]
    //       squaredNums.push(value * value)
    //   }
      
    // console.log(squaredNums.sort((a, b) => { return a - b})) 
  }
  
  sortedSquaredArray([1, 3, 5, 6, 7, 8])